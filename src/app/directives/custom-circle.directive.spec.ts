import { CustomCircleDirective } from './custom-circle.directive';

describe('CustomCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCircleDirective();
    expect(directive).toBeTruthy();
  });
});
